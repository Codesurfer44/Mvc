@using Mvc.Core.Editors
@model Mvc.Domain.Books

@{
    ViewData["Title"] = "Delete";
}

<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Books</h4>
    <hr />
    @Html.ShowFor(model => model.Title)
    @Html.ShowFor(model => model.Author)
    @Html.ShowFor(model => model.ReleaseDate)
    @Html.ShowFor(model => model.Price)
    @Html.ShowFor(model => model.Genre)
    @Html.ShowFor(model => model.Rating)
    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="Index">Back to List</a>
    </form>
</div>
